{{#partial "contenido" }}

<body>
  <div>
    <h1 class="cargar-text">Administrar Usuarios</h1>
<form method="POST" action="/editar-rol/{{id}}">
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Usuario</th>
          <th scope="col">Rol Actual</th>
          <th scope="col">Roles</th>
        </tr>
      </thead>
      <tbody>
            <tr>
                <td name="email" >{{usuario_email}}</th>
                <td name="rol">{{usuario_rol}}</th>
                <td>
                  <select name="selectRol" class="form-select">
                   <option selected value="Seleccione el nuevo rol" disabled>Seleccione el nuevo rol</option>
                   {{#each roles as |rol|}}
                   <option value="{{rol}}" >{{rol}}</option>
                   {{/each}}
                  </select>
                </td>
                </tr>
      </tbody>
    </table>
    <button method="post" action="/editar-rol/{{id}}" type="submit" class="btn btn-primary guardar-cambios">Guardar cambios</button>
    </form>
  </div>
  {{/partial}}
  {{> templates/base }}